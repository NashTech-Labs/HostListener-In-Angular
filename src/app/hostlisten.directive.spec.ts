import { HostlistenDirective } from './hostlisten.directive';

describe('HostlistenDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlistenDirective();
    expect(directive).toBeTruthy();
  });
});
